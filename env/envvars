---
# Ansible Runner 将继承启动 shell 的环境变量
# 该文件用于定义 Ansible Runner 运行时的环境变量
# 此文件（可以是 json 或 yaml 格式）表示 将在运行时添加到环境中的 环境变量
# 如果是自定义变量，需要使用`lookup('env', 'VAR_NAME')`来引用这些变量
# 例如：
ANSIBLE_REMOTE_PORT: 22
ANSIBLE_REMOTE_USER: demo
ANSIBLE_BECOME: true
ANSIBLE_BECOME_METHOD: sudo
ANSIBLE_BECOME_USER: root
ANSIBLE_HOST_KEY_CHECKING: False
# ANSIBLE_SSH_ARGS: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o PasswordAuthentication=yes'
